"""Application package root for the staged migration layout.

This package exists as the target structure for the refactor and intentionally does
not replace the legacy source tree yet. Old modules remain available until the
migration is validated end-to-end.
"""
